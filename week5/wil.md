## if 문
if문은 어차피 밥먹고 달달달달 하는거기 때문에 잘 한다.
text-decoration = line-through << 이거 하면 취소선 긋기 가능.
이거랑 if 문 겹쳐서 할일 완료하면 취소선긋기 가능할듯.  

## API 받아오기.
fetch로 api받을수 잇는데 fetch의 인자로는 url을 주기 떄문에 그 url 에다가 데이터를 요청하는것임.   
그러면 json 형식의 데이터를 return 해준다.  
then으로 비동기 처리가 가능함 .  
비동기처리? = 코드한줄씩 처리하면 너무 비효율적이니깐 서버에 데이터 요청한담에 기다리는동안 다른 코드 처리하는 방식.  
** 예시 ** 
'button.removeEventListener("click", onButtonClick);
 button.textContent = "완료!"; '
버튼이 눌리면 ,  버튼을 완료. 라고 바꿈.  
 button.setAttribute("disabled", true);
## 배포하는 방법.
니가알아서 잘해!!!.  